name: Test Code
on:
  pull_request:
    branches:
      - main

jobs:
  # unit tests
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: yarn install --immutable
      - run: yarn test

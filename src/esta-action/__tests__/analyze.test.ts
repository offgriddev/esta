import {analyze} from '../analyze'

describe(__filename, () => {
  it('calls getSourceFile', () => {})
  it('calls analyzeTypeScript', () => {})
  it('makes a directory', () => {})
  it('writes a file', () => {})
  it('returns the filename', () => {})
})

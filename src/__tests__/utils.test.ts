import {getSourceFile} from '../utils'

describe(__filename, () => {
  it('calls readdir', () => {})
  it('checks if path is a directory', () => {})
  it('excludes certain folders', () => {})
  it('calls itself recursively if a directory', () => {})
  it('includes in filePaths if filePath is valid', () => {})
  it('returns file paths', () => {})
})
